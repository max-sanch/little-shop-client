import React from 'react';
import {Typography} from "@mui/material";

const Shop = () => {
    return (
        <div>
            <Typography variant="h1" component="h2">
                Shop
            </Typography>;
        </div>
    );
};

export default Shop;